import { typesAiBudgetExpenses } from './types-ai-budget-expenses';

describe('typesAiBudgetExpenses', () => {
  it('should work', () => {
    expect(typesAiBudgetExpenses()).toEqual('types-ai-budget-expenses');
  });
});
