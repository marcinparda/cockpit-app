import { typesAiBudgetCategories } from './types-ai-budget-categories';

describe('typesAiBudgetCategories', () => {
  it('should work', () => {
    expect(typesAiBudgetCategories()).toEqual('types-ai-budget-categories');
  });
});
