import { typesSystemHealth } from './types-system-health';

describe('typesSystemHealth', () => {
  it('should work', () => {
    expect(typesSystemHealth()).toEqual('types-system-health');
  });
});
