export const environment = {
  // Whether we're in production mode
  production: true,
  // Base URL for API calls - change this to your production API endpoint
  apiUrl: 'https://your-production-api-url.com',
};
